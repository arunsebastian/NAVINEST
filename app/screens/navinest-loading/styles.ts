import { StyleSheet } from 'react-native';
const styles = StyleSheet.create({
    keyIn: {
        height: 40,
        margin: 12,
        borderWidth: 1,
        padding: 10
    }
});
export default styles;
